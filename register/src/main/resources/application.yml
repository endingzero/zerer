server:
  port: 8761
spring:
  application:
    name: zerer-eureka
eureka:
  instance:
    hostname: register
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname:localhost}:${server.port:8761}/eureka/
